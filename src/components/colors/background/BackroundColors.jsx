import React from "react";
import classNames from "classnames";
import * as classes from "./backroundColors.css";

const BackroundColors = () => {
  return <div className={classNames(classes.root)}></div>;
};

export default BackroundColors;
